import { BasicData } from "./basic-data"

export interface SearchTec{
    response_count: number
    exito: BasicData[]
    alkosto: BasicData[]
    linio: BasicData[]
    olimpica: BasicData[]
    jumbo: BasicData[]
    tcnocenter: BasicData[]
    resCat: string
}